22. Question

Category: CSAA – Design Secure Architectures

A company developed a meal planning application that provides meal recommendations for the week, as well as the food consumption of the users. The application resides on an Amazon EC2 instance, which requires access to various AWS services for its day-to-day operations.

Which of the following is the best way to allow the EC2 instance to access the Amazon S3 bucket and other AWS services?

- Add the API Credentials in the Security Group and assign it to the EC2 instance.
- `Create a role in IAM and assign it to the EC2 instance.`
- Use BatchGetSecretValue to retrieve long-term API credentials from AWS Secrets Manager and store them on the EC2 instance.
- Store the API credentials in a bastion host.

Exactly. **Question 22 is testing one very important AWS security principle:**

> **How should an EC2 instance securely get permission to use other AWS services?**

### What is the question saying?

Imagine your EC2 application needs to do this:

```text
EC2 Application
      |
      | "I need to read/write S3"
      ↓
Amazon S3
```

AWS needs to know:

> "Is this EC2 instance allowed to access S3?"

You have several possible ways to give it credentials, but AWS's best practice is:

**IAM Role → attach to EC2 → temporary credentials automatically provided to EC2**

genui{"finance_accounting_operations_learning_block":{"type_id":"DOUBLE_ENTRY_TRANSACTION_EFFECTS"}}

Ignore the widget above for this question; the key concept is IAM roles rather than accounting.


17-August-2026
