14. Question

Category: CSAA – Design High-Performing Architectures

A new online banking platform has been re-designed to have a microservices architecture in which complex applications are decomposed into smaller, independent services. The new platform uses Kubernetes, and the application containers are optimally configured for running small, decoupled services.

The new solution should remove the need to provision and manage servers, allow resources to be specified and paid for per application, and improve security through application isolation by design.

Which of the following is the MOST suitable solution to implement to launch this new platform on AWS?

- Host the application in Amazon EMR Serverless and Amazon EBS storage with the fast snapshot restore feature enabled.
- Use Amazon ECS to run the Kubernetes cluster on AWS Fargate.
- Deploy an Amazon EKS Cluster on AWS Outposts with Kubernetes Cluster Autoscaler and sync any orphaned pods with Amazon AppFlow.
- `Use AWS Fargate on Amazon EKS with Kubernetes Cluster Autoscaler to run the containerized banking platform.`

This question is testing a **very important EKS concept**. Let's break it down simply.

## Q14 — Correct answer ✅

> **Use AWS Fargate on Amazon EKS with Kubernetes Cluster Autoscaler**

The key is that the question gives you **three clues**:

### 1. "The application uses Kubernetes"

That immediately points to:

**Amazon EKS**

because:

* **EKS = managed Kubernetes**
* **ECS = AWS's own container orchestration service**

So don't choose ECS when the requirement explicitly says **Kubernetes**.

---

### 2. "Remove the need to provision and manage servers"

That points to:

**AWS Fargate**

Fargate is **serverless compute for containers**.

Normally:

```text
EKS
 │
 ├── EC2 Node
 ├── EC2 Node
 └── EC2 Node
       ↑
    You manage
```

With Fargate:

```text
EKS
 │
 ├── Pod → Fargate
 ├── Pod → Fargate
 └── Pod → Fargate

       ↑
   AWS manages
   the servers
```

You don't have to manage the underlying EC2 worker nodes.

---

### 3. "Application isolation"

Fargate provides strong workload isolation because each EKS pod runs in its own isolated compute environment.

So:

**Kubernetes + serverless + isolation**

→ **EKS + Fargate**

---

# 🧠 The most important distinction

You should memorize this:

### EKS + EC2

You manage the worker nodes.

```text
EKS
 ↓
EC2 Worker Nodes
 ↓
Pods
```

### EKS + Fargate

AWS manages the underlying compute.

```text
EKS
 ↓
Fargate
 ↓
Pods
```

So:

> **EKS tells you HOW Kubernetes is managed.**
> **Fargate tells you HOW the compute is provided.**

---

# What about Cluster Autoscaler?

This is the slightly confusing part of the question.

**Cluster Autoscaler** manages the number of **nodes** in a Kubernetes cluster.

For example:

```text
Pods need more capacity
        ↓
Cluster Autoscaler
        ↓
Add EC2 worker node
```

However, with **EKS on Fargate**, Fargate itself handles the underlying compute capacity for Fargate pods, so the question's explanation is somewhat dated/mixed in its treatment of Cluster Autoscaler.

For your **exam preparation**, follow the answer key you're using:

> **EKS + Fargate + Cluster Autoscaler**

But conceptually, remember:

**Cluster Autoscaler → nodes**

**Horizontal Pod Autoscaler → pods**

**Fargate → serverless container compute**

---

# 🔥 Very important EKS autoscaling map

This will help you with your earlier Question 21:

```text
                    EKS
                     │
          ┌──────────┴──────────┐
          │                     │
       PODS                   NODES
        │                       │
        ▼                       ▼
      HPA                 Cluster Autoscaler
        │                       │
        ▼                       ▼
 Scale number of            Scale number of
    Pods                      EC2 nodes
```

And:

```text
Karpenter
    ↓
Provision right-sized compute
    ↓
EC2 nodes
```

So remember:

> **HPA = Pods**

> **Cluster Autoscaler = Nodes**

> **Karpenter = Nodes/compute provisioning**

> **Fargate = Serverless compute for containers**

---

## Why the other answers are wrong

### ❌ ECS + Fargate

Fargate is correct, but **ECS isn't Kubernetes**.

```text
ECS → AWS container orchestration
EKS → Kubernetes
```

The question explicitly says Kubernetes → **EKS**.

---

### ❌ EKS on Outposts

Outposts brings AWS infrastructure into your own data center.

That means you're still dealing with physical infrastructure.

The requirement says:

> **remove the need to provision and manage servers**

So Outposts isn't the best fit.

---

### ❌ EMR Serverless

EMR is primarily for:

> **Big Data processing → Spark/Hadoop**

Not for running your Kubernetes banking platform.

---

# ⭐ Exam shortcut

When you see:

**"Kubernetes"**

→ **EKS**

Then ask:

**Do they want to manage EC2 worker nodes?**

* Yes → **EKS + EC2**
* No → **EKS + Fargate**

And if you see:

**"serverless containers + no servers + pay per application + isolation"**

→ **Fargate**

So this question can be compressed to:

> **Kubernetes + serverless containers = EKS + Fargate.**


14-August-2026


Yes — **don't try to memorize all of these separately yet.** They are related, and once you see the hierarchy, it becomes much easier.

Think of a container platform like this:

```text
                    CONTAINERS
                        │
             ┌──────────┴──────────┐
             │                     │
            ECS                   EKS
       AWS container           Kubernetes
       orchestration           managed by AWS
             │                     │
             └──────────┬──────────┘
                        │
                     COMPUTE
                        │
               ┌────────┴────────┐
               │                 │
              EC2             Fargate
          You manage          AWS manages
          servers             servers
```

Then **HPA, Cluster Autoscaler, and Karpenter** are about scaling.

---

# 1. ECS vs EKS

This is the first thing to understand.

### ECS = AWS's container orchestrator

ECS stands for:

> **Elastic Container Service**

It is AWS's own way of running and managing containers.

```text
Docker containers
       ↓
      ECS
       ↓
   AWS manages
   orchestration
```

You don't need Kubernetes to use ECS.

---

### EKS = Managed Kubernetes

EKS stands for:

> **Elastic Kubernetes Service**

If your company specifically wants **Kubernetes**, use EKS.

```text
Docker containers
       ↓
   Kubernetes
       ↓
      EKS
```

### 🧠 Memorize:

> **ECS = AWS containers**

> **EKS = Kubernetes**

That's enough for now.

---

# 2. EC2 vs Fargate

Now you've selected ECS or EKS.

You still need **compute** to actually run your containers.

You basically have:

### EC2

You use EC2 instances as the worker machines.

```text
EKS
 │
 ▼
EC2 Worker Nodes
 │
 ├── Pod
 ├── Pod
 └── Pod
```

You are responsible for the EC2 infrastructure.

---

### Fargate

Fargate is **serverless container compute**.

```text
EKS
 │
 ▼
Fargate
 │
 ├── Pod
 ├── Pod
 └── Pod
```

You don't manage the underlying servers.

### 🧠 Memorize:

> **EC2 = I manage the servers**

> **Fargate = AWS manages the servers**

---

# 3. Now comes scaling

Suppose your website normally has:

```text
5 Pods
```

But during Black Friday:

```text
500 Pods needed
```

Something needs to scale the application.

That's where **HPA** comes in.

---

# 4. HPA = Horizontal Pod Autoscaler

HPA means:

> **Horizontal Pod Autoscaler**

It scales the **number of Pods**.

```text
Normal traffic

3 Pods
🟦 🟦 🟦


High traffic

10 Pods
🟦 🟦 🟦 🟦 🟦
🟦 🟦 🟦 🟦 🟦
```

So:

> **HPA = more/fewer Pods**

### Example

CPU reaches 80%.

HPA says:

> "We need more Pods."

```text
3 Pods → 8 Pods
```

When traffic drops:

```text
8 Pods → 3 Pods
```

### 🧠 Memorize:

> **HPA = Pods**

---

# 5. Cluster Autoscaler

Now imagine your Pods need more machines.

You have:

```text
EC2 Node 1
   ├── Pod
   ├── Pod
   └── Pod

EC2 Node 2
   ├── Pod
   └── Pod
```

But now there isn't enough capacity for new Pods.

Cluster Autoscaler can add another node:

```text
EC2 Node 1
EC2 Node 2
EC2 Node 3  ← added
```

So:

> **Cluster Autoscaler = EC2 worker nodes**

### 🧠 Memorize:

**HPA → Pods**

**Cluster Autoscaler → Nodes**

---

# 6. Karpenter

Karpenter is also concerned with **nodes/compute**.

But it is more dynamic.

Suppose your Pods need:

```text
4 vCPU
16 GB RAM
```

Karpenter looks at the workload and says:

> "I'll provision appropriate compute for this workload."

It can dynamically provision EC2 instances suited to the Pods.

So:

> **Karpenter = dynamically provisions right-sized compute**

---

# The whole picture

This is the diagram I want you to memorize:

```text
                         EKS
                    Kubernetes
                         │
              ┌──────────┴──────────┐
              │                     │
             PODS                 NODES
              │                     │
              ▼                     ▼
             HPA          Cluster Autoscaler
        scales Pods        scales Nodes
                                    │
                                    ▼
                                Karpenter
                          provisions compute
```

And underneath EKS:

```text
             EKS
              │
       ┌──────┴──────┐
       │             │
      EC2         Fargate
   You manage    AWS manages
    servers       servers
```

---

# 🚨 One important correction

Don't think:

> "Karpenter = another type of HPA."

No.

They operate at different levels.

| Tool                   | What does it scale? |
| ---------------------- | ------------------- |
| **HPA**                | Pods                |
| **Cluster Autoscaler** | Nodes               |
| **Karpenter**          | Compute/nodes       |

---

# Your SAA cheat sheet

When you see this phrase → think this:

| Question keyword                                  | Answer             |
| ------------------------------------------------- | ------------------ |
| **Kubernetes**                                    | EKS                |
| **AWS container orchestration**                   | ECS                |
| **Serverless containers**                         | Fargate            |
| **No server management**                          | Fargate            |
| **Scale Pods**                                    | HPA                |
| **Scale worker nodes**                            | Cluster Autoscaler |
| **Dynamically provision right-sized EC2 compute** | Karpenter          |
| **Windows + SMB + AD**                            | FSx for Windows    |
| **HPC + ML + parallel filesystem**                | FSx for Lustre     |

### The 5 words I would memorize first:

**ECS → EKS → Fargate → HPA → Karpenter**

But don't memorize definitions yet.

Just remember the chain:

> **ECS/EKS = orchestration**
> **EC2/Fargate = compute**
> **HPA = Pods**
> **Cluster Autoscaler/Karpenter = Nodes/compute**

Once this is clear, the next 10–15 EKS questions will become **much easier**, because you'll stop seeing these as 6 unrelated AWS services.
