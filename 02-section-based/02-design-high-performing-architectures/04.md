4. Question

Category: CSAA – Design High-Performing Architectures

A leading IT consulting company has an application which processes a large stream of financial data by an Amazon ECS Cluster then stores the result to a DynamoDB table. You have to design a solution to detect new entries in the DynamoDB table then automatically trigger a Lambda function to run some tests to verify the processed data.

What solution can be easily implemented to alert the Lambda function of new entries while requiring minimal configuration change to your architecture?

- Use Systems Manager Automation to detect new entries in the DynamoDB table then automatically invoke the Lambda function for processing.
- Invoke the Lambda functions using SNS each time that the ECS Cluster successfully processed financial data.
- Use CloudWatch Alarms to trigger the Lambda function whenever a new entry is created in the DynamoDB table.
- `Enable DynamoDB Streams to capture table activity and automatically trigger the Lambda function.`


Exactly. This question is testing a very important **AWS event-driven pattern**.

### The simple mental model

**DynamoDB data changes → DynamoDB Streams → Lambda**

genui{"calculus_analysis_learning_block":{"type_id":"DERIVATIVE_PRODUCT_RULE","content":"(fg)' = f'g + fg'"}}

Ignore that widget here—this is not actually a calculus concept. The key AWS pattern is:

```text
ECS
 ↓
DynamoDB
 ↓
DynamoDB Streams
 ↓
Lambda
 ↓
Run tests
```

### Why DynamoDB Streams?

DynamoDB Streams records changes made to items in a DynamoDB table:

* INSERT
* MODIFY
* REMOVE

Lambda can consume those stream records and automatically execute when changes occur.

So if the question says:

> **"When something changes/is inserted in DynamoDB, trigger Lambda."**

Think immediately:

**DynamoDB Streams + Lambda**

### Why the other options are wrong

| Service                        | Think of it as                               |
| ------------------------------ | -------------------------------------------- |
| **DynamoDB Streams**           | "Something changed in my DynamoDB table"     |
| **CloudWatch Alarm**           | "A metric crossed a threshold"               |
| **SNS**                        | "Send/publish a notification/message"        |
| **Systems Manager Automation** | "Automate AWS operational/maintenance tasks" |

### 🧠 Exam shortcut

Whenever you see:

> **DynamoDB change → automatically invoke Lambda**

Answer:

**DynamoDB Streams**

This is one of those SAA patterns worth memorizing rather than overthinking.


14-August-2026
