1. Question

Category: CSAA – Design Secure Architectures

A company plans to conduct a network security audit. The web application is hosted on an Auto Scaling group of EC2 Instances with an Application Load Balancer in front to evenly distribute the incoming traffic. A Solutions Architect has been tasked to enhance the security posture of the company’s cloud infrastructure and minimize the impact of DDoS attacks on its resources.

Which of the following is the most effective solution that should be implemented?

- Configure Amazon CloudFront distribution and set a Network Load Balancer as the origin. Use VPC Flow Logs to monitor abnormal traffic patterns. Set up a custom AWS Lambda function that processes the flow logs and invokes Amazon SNS for notification.
- `Configure Amazon CloudFront distribution and set Application Load Balancer as the origin. Create a rate-based web ACL rule using AWS WAF and associate it with Amazon CloudFront.`
- Configure Amazon CloudFront distribution and set an Application Load Balancer as the origin. Create a security group rule and deny all the suspicious addresses. Use Amazon SNS for notification.
- Configure Amazon CloudFront distribution and set a Network Load Balancer as the origin. Use Amazon GuardDuty to block suspicious hosts based on its security findings. Set up a custom AWS Lambda function that processes the security logs and invokes Amazon SNS for notification.


Excellent. CloudFront questions are usually about **CloudFront + WAF + Shield + S3 + ALB**. This first question is testing **DDoS protection**, not CloudFront caching.

---

# Step 1: Architecture

```
Users
   │
   ▼
CloudFront
   │
   ▼
AWS WAF
   │
   ▼
Application Load Balancer
   │
   ▼
Auto Scaling EC2
```

---

# What is happening?

The company has:

* Auto Scaling EC2
* Application Load Balancer
* Internet-facing web application

They want to:

* improve security
* minimize DDoS attacks

The keyword is:

> **minimize DDoS attacks**

---

# Which AWS services protect websites?

AWS has several services.

### CloudFront

* CDN
* global edge locations
* caches content
* absorbs lots of traffic before it reaches origin

Think:

> First defense layer.

---

### AWS Shield

Protects against

* Layer 3
* Layer 4

attacks automatically.

Every CloudFront distribution already has **Shield Standard** for free.

---

### AWS WAF

Protects against

Layer 7 attacks

Examples:

* SQL Injection
* XSS
* HTTP Flood
* Bad Bots
* Too many requests

---

# Why WAF?

Imagine one attacker sends

```
5000 requests/second
```

Normal user

```
20 requests/minute
```

WAF can create a rule:

```
If IP > 1000 requests in 5 minutes

↓

Block
```

No EC2 resources wasted.

---

# Rate-based Rule

This is the most important keyword.

Example

```
IP A

Request 1
Request 2
...
Request 5000

↓

Threshold exceeded

↓

403 Forbidden
```

Automatically blocked.

---

# Why CloudFront + WAF?

Instead of traffic going

```
Internet
    │
ALB
    │
EC2
```

it becomes

```
Internet

↓

CloudFront

↓

WAF

↓

ALB

↓

EC2
```

Bad traffic is blocked **before** reaching ALB.

Less bandwidth

Less CPU

Less cost

Better security

---

# Why ALB instead of NLB?

Question says

```
Web Application
```

Web applications use

* HTTP
* HTTPS

These are Layer 7.

ALB understands

* URL
* Headers
* Cookies
* Hostnames

CloudFront integrates naturally with ALB.

---

NLB is used for

* TCP
* UDP
* TLS
* very high performance networking

Not for web-layer filtering.

---

# Why Option 2?

✅ CloudFront

Adds edge protection.

✅ ALB

Correct origin.

✅ WAF

Blocks attacks.

✅ Rate-based rules

Automatically mitigate HTTP floods.

Everything matches the requirement.

---

# Why others are wrong?

## Option 1

CloudFront

*

NLB

*

VPC Flow Logs

Flow Logs only **monitor** traffic.

They **cannot block** attacks.

❌ Wrong.

---

## Option 3

Security Group

Security Groups don't dynamically block attackers.

You would need to manually add IPs.

Not practical for DDoS.

❌ Wrong.

---

## Option 4

GuardDuty

GuardDuty only detects threats.

It does **not** sit inline to stop HTTP requests.

It generates findings, not protection.

❌ Wrong.

---

# Exam Memory Trick

Whenever you see

> DDoS

Immediately think:

```
CloudFront

+

AWS Shield

+

AWS WAF
```

---

Whenever you see

> HTTP Flood

Think

```
AWS WAF

Rate-Based Rules
```

---

Whenever you see

> CDN

Think

```
CloudFront
```

---

Whenever you see

> Static website globally

Think

```
S3

+

CloudFront
```

---

# One-line summary

* **CloudFront** = CDN + first layer of protection.
* **AWS Shield Standard** = automatic L3/L4 DDoS protection.
* **AWS WAF** = L7 protection (SQLi, XSS, HTTP flood).
* **Rate-based WAF rules** = automatically block IPs sending too many requests.
* For web apps, the standard architecture is **CloudFront → WAF → ALB → EC2**.
