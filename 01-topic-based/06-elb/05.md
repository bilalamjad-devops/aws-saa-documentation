5. Question

Category: CSAA – Design Secure Architectures

A company hosted an e-commerce website on an Auto Scaling group of Amazon EC2 instances behind an Application Load Balancer. The Solutions Architect noticed that the website is receiving a high number of illegitimate external requests from multiple systems with frequently changing IP addresses. To address the performance issues, the Solutions Architect must implement a solution that would block these requests while having minimal impact on legitimate traffic.

Which of the following options fulfills this requirement?

- Create a regular rule in AWS WAF and associate the web ACL to an Application Load Balancer.
- Create a custom network ACL and associate it with the subnet of the Application Load Balancer to block the offending requests.
- Create a private connection using AWS PrivateLink to block the offending requests.
- `Create a rate-based rule in AWS WAF and associate the web ACL to an Application Load Balancer.`

Excellent. You're now in the **ELB (Elastic Load Balancer)** section. This is one of the highest-weight topics in SAA. Here are the key concepts from Questions 1–5.

---

# ELB Summary (SAA Exam Notes)

## 1. Types of Load Balancers

### Application Load Balancer (ALB)

**Layer:** 7 (Application Layer)

Supports:

* HTTP
* HTTPS
* HTTP/2
* WebSocket
* gRPC

Best for:

* Web applications
* Microservices
* ECS
* Kubernetes
* Path-based routing
* Host-based routing

Exam keywords:

* URL path
* Hostname
* HTTP headers
* Cookies
* Query strings
* gRPC

→ Think **ALB**

---

### Network Load Balancer (NLB)

**Layer:** 4

Supports:

* TCP
* UDP
* TLS

Best for

* Millions of requests
* Very low latency
* Static IP
* Preserve client IP

Does NOT support

* Path routing
* Host routing
* gRPC

---

### Gateway Load Balancer (GWLB)

Layer 3 + Layer 4

Used for

* Firewalls
* IDS
* IPS
* Security appliances

Protocol:

GENEVE (6081)

Never choose GWLB for

* Web routing
* HTTP routing
* Path routing

---

# 2. Cross-Zone Load Balancing ⭐⭐⭐

Very common exam topic.

Without Cross-Zone

AZ-A

2 EC2

gets 50% traffic

Each receives

25%

AZ-B

8 EC2

gets 50%

Each receives

6.25%

Uneven distribution.

---

With Cross-Zone

Every load balancer node sends traffic to ALL instances.

2 + 8 = 10 EC2

Each receives

10%

Perfectly balanced.

---

Remember

ALB

✅ Always Enabled

NLB

❌ Disabled by default

GWLB

❌ Disabled by default

---

# 3. Path-Based Routing ⭐⭐⭐

Example

```
/api/android
```

↓

Android Target Group

```
/api/ios
```

↓

iOS Target Group

Uses

Listener Rules

↓

Path Conditions

Keywords

* URL
* URI
* /images/*
* /api/*
* /admin/*

Answer

ALB

---

# 4. Host-Based Routing

Example

```
api.company.com
```

↓

API Target Group

```
shop.company.com
```

↓

Shop Target Group

Keyword

Host Header

Again

ALB

---

# 5. gRPC Support ⭐⭐⭐

Many students miss this.

Question mentions

* gRPC
* HTTP/2
* Bidirectional Streaming

Answer

ALB

Select

Protocol Version

gRPC

---

# 6. ALB Access Logs ⭐⭐⭐

Question asks

Need

* Client IP
* Latency
* Request path
* HTTP request
* Response code

Answer

Enable

ALB Access Logs

Stored in

S3

Contains

* Client IP
* Request time
* URL
* Response code
* Backend latency
* Target latency

---

CloudTrail?

No.

CloudTrail logs

AWS API calls

NOT

HTTP Requests

---

# 7. Application Insights

Often paired with

ALB Access Logs

Used for

* Troubleshooting
* MTTR reduction
* Automatic dashboards
* Detect anomalies

Good companion service.

---

# 8. AWS WAF ⭐⭐⭐

Protects

* ALB
* CloudFront
* API Gateway
* AppSync

---

Two rule types

### Regular Rule

Match

* IP
* Header
* URI
* Country

---

### Rate-Based Rule ⭐⭐⭐

Exam keyword

* Too many requests
* DDoS-like traffic
* IP changes frequently
* Excessive requests

Answer

Rate-Based Rule

Works

Per IP

Example

1000 requests

per

5 minutes

↓

Temporarily blocks.

---

# 9. WAF vs NACL

Dynamic attackers

↓

AWS WAF

Static IP block

↓

NACL

Exam almost always prefers

WAF

---

# 10. Route 53 vs ELB

Route53

Routes users

↓

to

Load Balancer

Load Balancer

↓

Routes requests

↓

EC2

Never confuse them.

---

# 11. Global Accelerator

Purpose

Improve

Global Performance

Uses

AWS Global Network

Does NOT

* Replace ALB
* Do path routing
* Support gRPC

---

# Quick Decision Table

| Requirement                  | Service                   |
| ---------------------------- | ------------------------- |
| HTTP/HTTPS                   | ALB                       |
| Path routing                 | ALB                       |
| Host routing                 | ALB                       |
| gRPC                         | ALB                       |
| WebSocket                    | ALB                       |
| TCP                          | NLB                       |
| UDP                          | NLB                       |
| Static IP                    | NLB                       |
| Firewall appliance           | GWLB                      |
| Client IP + Latency logs     | ALB Access Logs           |
| Analyze API calls            | CloudTrail                |
| Too many requests            | WAF Rate-Based Rule       |
| Block based on header/IP/URI | WAF Regular Rule          |
| Equal traffic across AZs     | Cross-Zone Load Balancing |

---

# SAA Exam Traps

* **Path-based routing** → **ALB**
* **Host-based routing** → **ALB**
* **gRPC** → **ALB**
* **HTTP request logs** → **ALB Access Logs**
* **AWS API logs** → **CloudTrail**
* **Too many requests from changing IPs** → **WAF Rate-Based Rule**
* **Cross-zone load balancing** → Equal distribution across all AZs
* **NLB** = TCP/UDP + static IP + ultra-low latency
* **GWLB** = Firewalls/security appliances only

---

You're progressing well. You've already covered:

* ✅ DynamoDB
* ✅ EBS
* ✅ EFS
* ✅ ELB

These are some of the most frequently tested SAA topics. Continue reviewing them this way, and you'll build the pattern recognition needed to answer many exam questions quickly.
