
1. Question

Category: CSAA – Design High-Performing Architectures

A Solutions Architect is implementing a new High-Performance Computing (HPC) system in AWS that involves orchestrating several Amazon Elastic Container Service (Amazon ECS) tasks with an Amazon EC2 launch type that is part of an ECS cluster. The ECS cluster is configured with an ECS Capacity Provider to dynamically manage and scale the EC2 instances based on ECS task demand, ensuring sufficient compute resources are always available for high-throughput workloads.

Users around the globe will frequently access the system, and it is expected that hundreds of ECS tasks will run most of the time. These tasks require high-frequency read and write operations to a shared storage system. The output data of each ECS task is around 10 MB, and although obsolete data is eventually archived and deleted, the total storage size will not exceed 10 TB.

Which of the following is the MOST suitable solution that the Architect should recommend?

- Launch an Amazon DynamoDB table with DynamoDB Accelerator (DAX) and DynamoDB Streams enabled. Configure the table to be accessible by all ECS cluster instances. Set the DynamoDB table as the container mount point in the ECS task definition of the ECS cluster.

- `Launch an Amazon Elastic File System (Amazon EFS) with Provisioned Throughput mode and set the performance mode to Max I/O. Configure the EFS file system as the container mount point in the ECS task definition of the ECS cluster.`

- Set up an SMB file share by creating an Amazon FSx for Windows File Server. Set the file share as the container mount point in the ECS task definition of the ECS cluster.

- Launch an Amazon Elastic File System (Amazon EFS) file system with Bursting Throughput mode and set the performance mode to General Purpose. Configure the EFS file system as the container mount point in the ECS task definition of the ECS cluster.

<img width="1432" height="1135" alt="Amazon+EFS+-+Performance+and+Throughput+mode+-+SAA-C02" src="https://github.com/user-attachments/assets/caf7d0ba-e43f-4b0d-b56b-ac1d6d63dd02" />


Excellent. Welcome to **Amazon EFS**. This very first question already teaches about **Performance Mode** and **Throughput Mode**, which many students confuse.

---

# Step 1: What is EFS?

Think of EFS as a **shared network drive**.

Instead of:

```text
EC2 1
   │
 EBS
```

You have:

```text
          EFS
      /    |    \
     /     |     \
 ECS1    ECS2   ECS3
```

Every ECS task (or EC2) can read/write to the **same files** simultaneously.

That is why EFS is perfect for:

* Shared application data
* Containers
* WordPress uploads
* Shared configuration
* HPC workloads

---

# Why not EBS?

One EBS volume normally attaches to **one EC2**.

Here there are:

> Hundreds of ECS tasks

They all need access to the same storage.

Therefore:

❌ EBS

✅ EFS

---

# The question now asks something different

Not:

> Which storage?

We already know:

✅ EFS

It asks:

> **Which EFS configuration?**

There are two things to configure.

---

# 1. Performance Mode

There are only TWO.

## General Purpose

Default

Use for:

* websites
* WordPress
* CMS
* normal applications

Low latency

Small to medium workloads

---

## Max I/O

Use when

Many servers

Many containers

Many clients

High parallel access

Example:

```text
500 ECS Tasks

↓

Reading

↓

Writing

↓

Same EFS
```

General Purpose starts becoming a bottleneck.

Use

✅ Max I/O

---

# Easy memory trick

Normal workload

↓

General Purpose

Massive parallel workload

↓

Max I/O

---

# 2. Throughput Mode

Again only TWO (for SAA)

---

## Bursting Throughput

Default

Throughput increases automatically based on storage size.

Example

You store

```text
100 GB
```

↓

Small throughput

Store

```text
5 TB
```

↓

Higher throughput

---

Good for

Traffic goes

```text
Low

↓

High

↓

Low

↓

High
```

Spiky workloads.

---

## Provisioned Throughput

Suppose

You only have

```text
100 GB
```

But

Need

```text
500 MB/s
```

all day.

Bursting cannot provide this continuously.

So you pay for

Guaranteed throughput.

That is

Provisioned Throughput.

---

Memory trick

Bursting

↓

Occasional spikes

Provisioned

↓

Constant heavy traffic

---

# Read the scenario carefully

The question says

> users around the globe

means

Large workload.

Then

> hundreds of ECS tasks

means

Many parallel clients.

Then

> high-frequency reads and writes

means

Heavy I/O.

Then

> most of the time

This sentence is VERY important.

It means

NOT spikes.

It is continuous.

Therefore

Bursting ❌

Provisioned ✅

---

Also

Hundreds of ECS tasks

↓

General Purpose ❌

↓

Max I/O ✅

---

# Why not Bursting?

Imagine

```text
Morning

500 tasks

Night

10 tasks
```

This is bursting.

AWS can burst.

---

But here

```text
Morning

500 tasks

Afternoon

500 tasks

Night

500 tasks
```

Always busy.

Need guaranteed throughput.

Provisioned.

---

# Why not DynamoDB?

Because DynamoDB is

A database.

Not

A file system.

Question says

Container mount point.

Only

EFS

can be mounted like

```bash
/mnt/shared
```

---

# Why not FSx?

FSx Windows

=

Windows SMB file sharing.

The scenario

doesn't mention

Windows.

It mentions

ECS containers.

EFS is AWS's native shared file system for Linux/container workloads.

---

# Exam Keywords

### EFS

* Shared file system
* Multiple EC2
* Multiple ECS tasks
* Multiple AZs
* NFS

---

### General Purpose

Default

Low latency

Normal applications

---

### Max I/O

Huge parallel access

Hundreds/thousands of clients

HPC

---

### Bursting Throughput

Default

Traffic spikes

---

### Provisioned Throughput

Constant heavy throughput

---

# Decision Tree

```text
Need shared storage?

↓

Yes

↓

EFS

↓

Many users simultaneously?

↓

Yes

↓

Max I/O

↓

Traffic always heavy?

↓

Yes

↓

Provisioned Throughput
```

---

## ⭐ SAA Exam Tip

When you see phrases like:

* **hundreds of ECS tasks**
* **thousands of EC2 instances**
* **high-performance computing (HPC)**
* **constant high throughput**
* **shared storage**
* **frequent read/write operations**

your brain should immediately think:

**EFS + Max I/O + Provisioned Throughput**.

This pattern appears frequently in AWS architecture questions.
