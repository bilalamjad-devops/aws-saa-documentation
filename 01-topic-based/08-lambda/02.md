2. Question

Category: CSAA – Design High-Performing Architectures

A media company wants to ensure that the images it delivers through Amazon CloudFront are compatible across various user devices. The company plans to serve images in WebP format to user agents that support it and return to JPEG format for those that don’t. Additionally, the company wants to add a custom header to the response for tracking purposes.

As a solution architect, what approach would one recommend to meet these requirements while minimizing operational overhead?

- Implement an image conversion service on Amazon EC2 instances and integrate it with CloudFront. Use AWS Lambda functions to modify the response headers and serve the appropriate format based on the User-Agent header.
- Create multiple CloudFront distributions, each serving a specific image format (WebP or JPEG). Route incoming requests based on the User-Agent header to the respective distribution using Amazon Route 53.
- Generate a CloudFront response headers policy. Utilize the policy to deliver the suitable image format according to the User-Agent HTTP header in the incoming request.
- `Configure CloudFront behaviors to handle different image formats based on the User-Agent header. Use Lambda@Edge functions to modify the response headers and serve the appropriate format.`


<img width="903" height="487" alt="Amazon-CloudFont-Lambda-26-06-2023" src="https://github.com/user-attachments/assets/7861d3a3-1094-435f-bb6c-62a34d4407a9" />


This is a **Lambda@Edge** question, not a regular Lambda question.

---

# Keywords

* CloudFront
* User-Agent
* WebP vs JPEG
* Modify Response Header
* Lowest operational overhead

Immediately think:

👉 **Lambda@Edge**

---

# What is Lambda@Edge?

Lambda@Edge lets you run Lambda **at CloudFront Edge Locations**.

Instead of:

User → AWS Region

It becomes:

User → Nearby CloudFront Edge → Lambda@Edge

This reduces latency.

---

# What can Lambda@Edge do?

It can inspect or modify:

* Viewer Request
* Origin Request
* Origin Response
* Viewer Response

Examples:

* Redirect users
* Authentication
* URL rewrite
* Image optimization
* Device detection
* Add custom headers
* Country-based content
* A/B testing

---

# Scenario

Requirement 1

If browser supports

WebP

↓

Serve WebP

Else

↓

Serve JPEG

Need to inspect

User-Agent

Requirement 2

Add custom response header

Lambda@Edge can do both.

---

# Correct Answer

✅ Configure CloudFront behaviors and use **Lambda@Edge** to:

* Read User-Agent
* Serve correct image format
* Modify response headers

---

# Why others are wrong

### EC2 Image Service

Possible

But

High operational overhead

Need EC2 management.

Question asked

Lowest operational effort.

---

### Multiple CloudFront Distributions

Works

But unnecessary.

One distribution + Lambda@Edge is simpler.

---

### Response Headers Policy

Can

Add headers

Cannot

Read User-Agent

Cannot

Change image format dynamically.

---

# Exam Shortcut

Whenever CloudFront needs to:

* Inspect request
* Inspect User-Agent
* Change URL
* Add/remove headers
* Redirect users
* Customize response

Think

✅ **Lambda@Edge**

---

# CloudFront Response Headers Policy vs Lambda@Edge

| Feature            | Response Headers Policy | Lambda@Edge |
| ------------------ | ----------------------- | ----------- |
| Add static headers | ✅                       | ✅           |
| Read User-Agent    | ❌                       | ✅           |
| Change content     | ❌                       | ✅           |
| Dynamic logic      | ❌                       | ✅           |
| Authentication     | ❌                       | ✅           |

---

# Memory Trick

**CloudFront + Dynamic Logic = Lambda@Edge**

**CloudFront + Static Headers Only = Response Headers Policy**

---

## Lambda vs Lambda@Edge

| Lambda                                                         | Lambda@Edge                                |
| -------------------------------------------------------------- | ------------------------------------------ |
| Runs in AWS Region                                             | Runs at CloudFront edge locations          |
| Triggered by AWS services (S3, API Gateway, EventBridge, etc.) | Triggered by CloudFront requests/responses |
| General serverless compute                                     | CloudFront request/response customization  |

### SAA Exam Keywords

* **CloudFront + User-Agent** → Lambda@Edge
* **CloudFront + Custom Headers (dynamic)** → Lambda@Edge
* **CloudFront + Redirects/URL Rewrite** → Lambda@Edge
* **CloudFront + Device Detection** → Lambda@Edge
* **CloudFront + Static Security Headers** → Response Headers Policy

 
